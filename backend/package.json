{
  "name": "@deeix/api",
  "version": "0.3.6",
  "private": true,
  "scripts": {
    "build": "make build",
    "check": "make check-version && go vet ./...",
    "clean": "rm -rf ../.cache/deeix-chat",
    "dev": "make run",
    "test": "make test",
    "test:agent-group-contract": "go test ./internal/domain/conversation ./internal/transport/http/conversation -run \"AgentGroup.*Model\" -count=1"
  }
}
